#!/bin/bash
. /bin/load-envd

confd -onetime -backend env

if [ "$1" = "init" ]; then
	echo "Running Init..."
	run-parts /docker/container-init.d

elif [ "$1" = "shell" ]; then
	echo "Running shell"
	bash
else
	echo "Starting dinit Periodic..."
	exec /bin/dinit -r /bin/periodic-sh -r "$@"
fi
